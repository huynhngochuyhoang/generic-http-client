# 2.0 Cleanup

Roadmap item: `3.2 Remove deprecated log-body property`.

Suggested release: `2.0.0`.

## Scope

- Remove deprecated `reactive.http.clients.*.log-body` compatibility.
- Keep `reactive.http.clients.*.log-exchange` as the single exchange-logging property.

## Acceptance

- [ ] Remove `logBody` field, getter, and setter from `ReactiveHttpClientProperties.ClientConfig`.
- [ ] Simplify `isExchangeLoggingEnabled()` to `return logExchange`.
- [ ] Remove `log-body` metadata from `additional-spring-configuration-metadata.json`.
- [ ] Update README, docs, and `CHANGELOG.md` with a migration note.
- [ ] Bump project version to `2.0.0`.
- [ ] `mvn test` passes.
