# 2.0 Cleanup

Roadmap item: `3.2 Remove deprecated log-body property`.

Suggested release: `2.0.0`.

## Scope

- Remove deprecated `reactive.http.clients.*.log-body` compatibility.
- Keep `reactive.http.clients.*.log-exchange` as the single exchange-logging property.

## Acceptance

- [x] Remove `logBody` field, getter, and setter from `ReactiveHttpClientProperties.ClientConfig`.
- [x] Simplify `isExchangeLoggingEnabled()` to `return logExchange`.
- [x] Remove `log-body` metadata from `additional-spring-configuration-metadata.json`.
- [x] Update README, docs, and `CHANGELOG.md` with a migration note.
- [x] Bump project version to `2.0.0`.
- [x] `mvn test` passes.
